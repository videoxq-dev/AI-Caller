# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_caller

# Better Auth
BETTER_AUTH_SECRET=replace-with-at-least-32-random-characters
BETTER_AUTH_URL=http://localhost:3000

# Provider credential encryption
# Generate with: openssl rand -base64 32
INTEGRATION_ENCRYPTION_KEY=replace-with-base64-encoded-32-byte-key

# Background jobs
PG_BOSS_SCHEMA=pgboss

# Transactional email (password reset + JVZoo welcome email)
SMTP_URL=smtp://username:password@smtp.example.com:587
SMTP_FROM=AI Caller <no-reply@example.com>

# JVZoo commerce
JVZOO_IPN_SECRET=replace-with-your-jvzipn-secret-key
JVZOO_CORE_PRODUCT_IDS=123456
STARTER_CREDITS=2500

# Calendar OAuth applications
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
MICROSOFT_OAUTH_CLIENT_ID=
MICROSOFT_OAUTH_CLIENT_SECRET=

# Meta Tech Provider / WhatsApp Embedded Signup
# App ID + App Secret: Meta Developer Dashboard -> App settings
# Config ID: Facebook Login for Business / WhatsApp Embedded Signup configuration
META_APP_ID=
META_APP_SECRET=
META_EMBEDDED_SIGNUP_CONFIG_ID=
# Meta's current Tech Provider sample uses v22.0; keep this explicit so upgrades are deliberate.
META_GRAPH_API_VERSION=v22.0
# Secret value configured for the Meta webhook verification challenge.
META_WEBHOOK_VERIFY_TOKEN=
# Optional 6-digit PIN used when the selected Embedded Signup flow requires phone registration.
META_PHONE_REGISTRATION_PIN=

# Logging
LOG_LEVEL=info
